// Vitest stub for Next.js `server-only`.
// In Next.js runtime, importing "server-only" enforces server-only module usage.
// For unit tests, we replace it with an empty module via vitest alias.

export {}

